import { RouteLoadingFallback } from "@/components/navigation/route-loading-fallback";

export default function DashboardLoading() {
  return <RouteLoadingFallback label="Préparation de votre espace…" />;
}
