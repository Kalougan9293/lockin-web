import { fredoka } from "@/lib/fonts/fredoka";

/** Titre principal du tableau — chaleur de marque (Fredoka). */
export const dashboardTableTitleClassName = `${fredoka.className} text-4xl font-bold leading-none tracking-tight sm:text-5xl`;

/** Bouton Configurer — sobre et B2B (Geist). */
export const dashboardConfigureTitleClassName =
  "font-sans text-2xl font-semibold leading-none tracking-tight sm:text-3xl";

/** En-têtes de colonnes du tableau. */
export const dashboardColumnHeaderClassName =
  "font-sans text-sm font-semibold tracking-tight";

export const dashboardTitleGradientClassName =
  "bg-gradient-to-r from-violet-300 via-fuchsia-300 to-indigo-300 bg-clip-text text-transparent";
